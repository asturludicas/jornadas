# index.html (único archivo)
├── CFG {}           → configuración editable arriba del todo
├── <style>          → todo el CSS
├── Login screen     → pantalla de acceso
├── App shell        → sidebar + páginas admin e inscrito
├── Modales          → persona, grupo, pago, habitación
└── <script>
    ├── DB {}        → estado en memoria
    ├── JSONBin      → lectura/escritura remota
    ├── LocalStorage → caché y sesión
    ├── CSV parser   → para importación
    ├── Admin pages  → dashboard, personas, grupos, pagos...
    └── Inscrito     → portal personal + alojamiento
